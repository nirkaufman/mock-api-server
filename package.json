{
  "name": "mock-api-server",
  "version": "0.0.1",
  "description": "Simple mock api server with auth, for teaching only",
  "scripts": {
    "start": "node src/server.js",
    "db": "json-server --watch data/database.json"
  },
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "json-server": "^0.14.2",
    "jsonwebtoken": "^8.1.0"
  }
}
